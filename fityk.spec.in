# may not work

# You are welcome to become a maintainer of binary fityk package for your 
# favourite Linux distribution.

Summary:        A program for fitting and analyzing data.
Name:           @PACKAGE@
Version:        @VERSION@
Release:        1
Group:          Applications/Engineering
License:        GPL
Vendor:         Marcin Wojdyr 
URL:            http://www.unipress.waw.pl/fityk/
Source:         http://downloads.sourceforge.net/fityk/%{name}-%{version}.tar.bz2
BuildRoot:      /tmp/%{name}-%{version}-root
BuildRequires:  wxGTK-devel
Requires:       wxGTK
# - it also requires readline library
# - it does NOT require boost library, because it uses only header files
#   from boost, and these headers are distributed in fityk tarball.
# - gnuplot may be useful, but it is not required, and majority of users
#   will never use it anyway.

#is this necessary?
Prefix: /usr

#is this necessary?
%define                 _datadir        %{_prefix}/share/fityk

%description
Fityk is a program for nonlinear fitting of analytical functions 
(especially peak-shaped) to data (usually experimental data).
Can be also used for visualization of x-y data only.

%prep
%setup -q

%build
export LDFLAGS="-s"
%configure 
make 

%install
rm -rf $RPM_BUILD_ROOT
%makeinstall 

%clean
rm -rf $RPM_BUILD_ROOT

%files
%defattr(644,root,root,755)
%doc AUTHORS ChangeLog COPYING INSTALL NEWS README TODO 
# *.xml *.xsl *.html?
%attr(755,root,root) %{_bindir}/fityk 
%attr(755,root,root) %{_bindir}/cfityk 
%{_datadir}
%{_libdir}/*
%{_includedir}/*
%{_mandir}/man1/*
# there are also other files you may want to install, e.g. fityk.desktop,
# fityk.png

