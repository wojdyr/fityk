This directory contains files, that are used by program to display
on-line documentation and files that are used to produce documentation. 
If you just want to read documentation, the easiest way is to run program 
and read help or visit fityk website.

.htb file is a renamed .zip file. It is used by help viewer in fityk.

fitykhelp.xml is a DocBook (XML) file. It can be converted to various
formats (html, HtmlHelp, PDF etc.)

Math formula images are created with LyX. See file math.lyx in CVS.

